server {
    listen {{ nginx_stats_listen | default(8080) }};
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:8404/;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}
